# Vizuara-NL-SQL-Project
A lightweight SQL database agent built from scratch using the ReAct (Reasoning + Acting) methodology. Interprets natural language queries and safely executes read-only database operations through structured thought-action-observation loops. Supports multiple LLM providers with full execution tracing and robust validation.
